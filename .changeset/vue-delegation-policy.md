---
"@naculus/connect-appkit-vue": patch
---

Add Vue `useDelegationPolicy` over appkit-core's shared delegation-policy
flow, with caller-supplied signer, signature verifier, execution planner and
delegation state.
